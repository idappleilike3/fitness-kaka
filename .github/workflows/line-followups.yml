name: LINE paid member follow-ups (Supabase managed)

on:
  workflow_dispatch:

jobs:
  scheduler-status:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm scheduler ownership
        run: echo "Automatic paid LINE meal follow-ups are scheduled by Supabase Cron."
